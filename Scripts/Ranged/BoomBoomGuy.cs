using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoomBoomGuy : RangedUnit {

	// Use this for initialization
	void Start () {
        //changing the stats of the unit
        hP = 50;
        atk = 50;
        atkRange = 3;
        speed = 1;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
