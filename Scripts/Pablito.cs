using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pablito : RangedUnit {

	// Use this for initialization
	void Start () {
        //changing the stats of the unit
        hP = 50;
        atk = 10;
        atkRange = 5;
        speed = 1;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
